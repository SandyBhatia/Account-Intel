/** Bumped whenever a package is cut. Rendered in the top bar so it is
 *  possible to tell at a glance which build is actually deployed —
 *  a deploy that silently did not happen looks identical otherwise. */
export const BUILD = "brief-1.0";
